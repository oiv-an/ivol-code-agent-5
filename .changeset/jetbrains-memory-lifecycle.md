---
"ivol-code-agent-5": patch
---

Fix PhpStorm memory leaks from disconnected agent sessions and closed editor tabs during long-running work.

Use the IDE-provided Kotlin runtime instead of bundling incompatible legacy transitive libraries.
